import styled from '@emotion/styled';

const StyledContainer = styled.div`
  min-width: 300px;
  text-align: center;
`;

export const Popup = () => {
  return <StyledContainer>❤️</StyledContainer>;
};
